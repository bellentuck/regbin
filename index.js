module.exports = {
  regbin: require('./regBin'),
  //defaults: require('./defaultsWrapper')
}
// a = async (fn, input) => {
//   ans = await fn(input);
//   console.log('the answer is... ', ans);
// }
